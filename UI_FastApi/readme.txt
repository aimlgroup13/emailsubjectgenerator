pip install -r requirements.txt

#open 2 terminals 

#on first terminal
#once above is installed, to start fastapi use the cmd:
#uvicorn email_summary_api:app --reload

#on second terminal
#to start the streamlit app:
#streamlit run ui.py

---------------------

